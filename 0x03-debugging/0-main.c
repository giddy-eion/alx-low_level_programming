#include "main.h"

/**
 * main - checks if 0 is an integer
 *
 * Description: it uses the func protoype positive_or_negative
 * Return: 0 if successful
 */
int main(void)
{
	int i = 0;

	positive_or_negative(i);

	return (0);
}
