#include "main.h"

/**
 * reverse_array - reverse tge content of an array of int
 * @a: pointer of type int
 * @n: int
 */
void reverse_array(int *a, int n)
{
	int tmp, index, unk;

	tmp = *a;
	index = n;
	for (unk = index - 1; unk >= 0; unk--)
		a[unk];
}
