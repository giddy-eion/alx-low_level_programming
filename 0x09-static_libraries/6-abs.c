#include "main.h"

/**
 * _abs - int n
 * @n: absolute value
 *
 * Description: computes the abosolute value of an integer
 * Return: n if postive or 0
 * -n if negative
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
