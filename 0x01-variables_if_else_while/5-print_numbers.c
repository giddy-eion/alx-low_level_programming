#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints 0 to 9 in base 10
 * Return: 0
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)

	{
		printf(a);
	}
	printf('\n');
	return (0);
}
