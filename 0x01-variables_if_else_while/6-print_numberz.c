#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints 0 to 9 using only putchar
 * Return: 0
 */
int main(void)
{
	int num;
	
	if (num = 0; num < 10; num++)
	{
		putchar((num % 10) + '0');
	}
	putchar('\n');
	return (0);
}
